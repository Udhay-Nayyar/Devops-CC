Why Kubernetes ?
    ----------
        
Know about Monolithic and Microservices
           ----------     -------------
   all the services         all the service are now independent from each other 
   are kept at one place



if you want to manage these services how do we do that ----> Kubernetes

Kubernetes Architecture ?

Master Node             
API server -  communication channel between the master and worker   
controller manage -  manages things at the master side
scheduler  - this will schedule the formation of the container              
etcd - act as the database on the master side




Worker side 
Pods - it is like a unit where grp of container runs togther 
Server Proxy - some one from out side the organisation want to access the conatiner services will take help from this server proxy 
Kubelet - acts as the manager on the worker side

How do these node communicate with each other 
using the CNI network


if you want to perform anything on these node take help of the kubectl
 


------- Try making it on the paper with any reference --------

First lets learn how to make the cluster 


First what is cluster
it is the group of the nodes i.e 1 master and  M worker node 



How many ways of creating the cluster 

Kubeandm
Minikube DONE
KIND Cluster DONE
EKS / AKS / GKE




Lets start with KIND Cluster that to locally
install kubectl , docker , kind via the repo -https://github.com/LondheShubham153/kubestarter.git

now for this you should do it all practice the  making of the cluster that to local 


After you have practiced making the cluster now the next thing 


Docker Containers
-----------------
       |
       |
      Pods
       |
       |
     deployment
       |
       |
     services
       |
       |
     Sweet User 
-----------------------------------------------
                |
                |
                |
Kubernetes will have many of these not only one
due to which we will have the issue that in kubernetes soo many pods , services will be very hard to maintain right 
Solution Namespace



> kubectl get ns 
> kubectl get namespace
------------------------ read how many name space we have

Note we also have a default name space called as default 

> kubectl get pods
------------------------ ready kro bhai kitna pods are running right now 
                         this will go search for the pods in the default namespace 
but if you want to check the pods in the particular namespace then the commands become
> kubectl get pods -n "name of the namespace"


how do we create a name space ?
> kubectl create ns "name of the namespace"



now in the namespace you have just created we will create the pods , deployment , services in this namespace 
so lets goo



> kubctl run nginx --image=nginx
-------------------------------- This commads helps us to create the pods for the nginx 
their is a catch here that this will create a pod no doubt but this will add the pods in the default name space
so in order to avoid that we will use the command that is written below 

> kubctl run nginx --image=nginx -n nginx
                                    -----
> kubectl delete pod nginx  



now try to locate the nginx pod in the nginx namespace 
using command
> kubectl get pods -n nginx


############################# I know these commands are not feel good because it very manual work  in order to avoid that we will make yaml file that consist of all the commands (automation) yml or manifest file is what we make ################################





describe command 
exec command to get into the pods 


deployment ?
create a yml file for this 


Note when we need grp of pods if the traffic is going to be  huge (big billion days sale)

First understant the work Replication Controller ?
This gods child is resposible for managing the replicas of the pods 



we can achive this using the ReplicaSet | StatefulSet |  Deployement (Rolling Updates)
                             ---------------------------------------
                             all of these do the same task but in different style 


check the yml file for the developement at the time frame of 1:52:00


One question might have come after creating the manifest file how do we run that 
>kubectl apply -f name_of_the_file.ymlm 
 

what if i want to delete that manifest file ?
> kubectl delete -f name_of_the_file.yml
  

after this try to ready the deployment and pods using get commands




Now finally time has come to scale the container 
> kubectl scale deplovemnt/nginx-development -n nginx --replicas=5

now go check the pods in specfic namespace and be happy baby 


Time to practice the rolling updates
> kubctl get pods -n nginx -o wide 
 --------------------------------- if you want the more information about the pods this is just like ls ka ls -l version 


we  will use kubectl roll out command



make a case where you have mutilple pods 

> kubectl set image deployment/nginx-deployment -n nginx nginx=nginx:1.27.3
> kubectl get pods -n nginx 

Now what is rolling updates ?
it will never close or shutdown all the containers at once 1


Time to apply the replica set it will be almost same yml  file as of the development just change the dev to rep 
and rolling update feature will be misssing 
 


Deamnset 
atleast one pod must be running at each node 
Try it practically 




What is Jobs >> it is a task which we accept to get over after its job is completed not the task which goes on forever

Try practically using jobs and                                 cronjobs 
                      ----                                     --------
                     single job to be done and finish          single job needs to be done a particular time again and again 













